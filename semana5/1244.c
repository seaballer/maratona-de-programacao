#include <stdio.h>
#include <string.h>

char p[51][51];

int main()
{
    int n, i, j;
    char frase[2502];
    
    scanf("%d", &n);
    
    while (n--)
    {
        scanf(" %[^\n]\n", frase);
    }

    return 0;
}