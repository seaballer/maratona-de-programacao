#include <stdio.h>

int v[10001];
int c[10001];
int t[10001];

int main()
{
    int n, m, i, j, soma = 0;

    scanf("%d %d", &n, &m);
    
    for (i = 1; i <= n; i++)
    {
        scanf("%d", &v[i]);
    }
    for (i = 1; i <= m; i++)
    {
        scanf("%d", &c[i]);
    }

    for (i = 1; i <= m; i++)
    {
        
    }

    return 0;
}